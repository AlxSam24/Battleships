#include "Ship.h"
#include <iostream>
#include <string>
using namespace std;

Ship::Ship(string name, int length, char symbol)
    :name_(name), length_(length), symbol_(symbol)
{
}
void Ship :: registerHit() {
    //Finish implementing methods
}
